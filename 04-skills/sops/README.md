# SOPs — written-out jobs

Every worker gets a written-out job with steps, so the OS knows exactly what
"run the job" means. Each SOP: title, summary, steps.

## Pattern (copy this for new SOPs)
```yaml
id: sop-<agent>
title: <what it does>
summary: <one line>
steps:
  - <step 1>
  - <step 2>
